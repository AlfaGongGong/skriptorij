# BOOKLYFI TURBO V10 — Python ovisnosti
# Instaliraj s: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────────────────────
flask>=3.1.0

# ── HTTP klijent (API pozivi prema AI provajderima) ───────────────────────────
requests>=2.32.0

# ── EPUB parsiranje i obrada ──────────────────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=6.0.0
ebooklib>=0.18

# ── Opcionalno: MOBI format podrška ──────────────────────────────────────────
# mobi>=0.3.0

# ── Opcionalno: morfološki validator (bosnički NLP) ───────────────────────────
# Potreban samo za Korak 2 morfološku validaciju.
# Instaliraj zasebno ako je potrebno:
#   pip install classla==2.2.1 obeliks==1.1.6
# classla==2.2.1
# obeliks==1.1.6

# ── Testovi ───────────────────────────────────────────────────────────────────
pytest>=9.0.0
